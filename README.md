# faculdade-chatbot-t1

# Create virtual python env
python3 -m venv venv

# define Venv as source
source ./venv/bin/activate

# install dependencies through requirements
source venv/bin/activate && python ./index.py